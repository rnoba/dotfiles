#!/usr/bin/env bash

pushd "$HOME/Public/Dotfiles/packages/Spotf" >/dev/null
if [[ ! -f ./.ENV/bin/activate ]]; then
  python -m venv ./.ENV
fi
source ./.ENV/bin/activate
python -c "import dbus" &> /dev/null || pip install dbus-python 

./main.py "$@"
popd >/dev/null
